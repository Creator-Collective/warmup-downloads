add dashboard shortcut to chrome extensions

misc:
- add fixed trusted-origin manager handoff using the installed extension
- bound connection and opening waits with truthful menu fallback
- reject framed, stale and spoofed responses without automatic retries
- publish helper assets without an extension version or permission change
- verify helper regression checks and independent reviews
