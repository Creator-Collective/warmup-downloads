fix tiktok tab discovery and web session controls

misc:
- refresh idle platform tabs so newly opened tiktok sessions can start
- retain selected tabs and discard obsolete discovery responses
- forward validated platform choices through the public warmup bridge
- allow the existing open-platform action from trusted web controls
- release version 0.6.34 with matching downloads and setup notes
- cover delayed tabs, selection safety, recovery and bridge boundaries
